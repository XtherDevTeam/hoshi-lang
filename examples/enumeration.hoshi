use runtime "runtime"

enum ServerState {
    Running,
    Stopped,
    Error = 4
}

func main() : int {
    let status = ServerState.Running
    if (status != ServerState.Error) {
        runtime.puts("enumeration validation passed")
    }
    return ServerState.Running
}
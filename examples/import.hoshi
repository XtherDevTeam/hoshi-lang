/**
 * This is an example of importing a function from a library.
 * Comment supported!
 */

import puts(str : ptr) : int32 from "builtin"

func main() : int {
    puts("Hello, C-style world! This is main() from hoshi-lang!")
    puts("FFI validation completed.")
    return 233
}